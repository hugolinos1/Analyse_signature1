
// Worker PDF.js compatible avec Vite et les modules ES
// Version simplifiée qui utilise directement le CDN

self.onmessage = function(e) {
  // Rediriger vers le vrai worker PDF.js depuis le CDN
  importScripts('https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.0.379/pdf.worker.min.js');
};
